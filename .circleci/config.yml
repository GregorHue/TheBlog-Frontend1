version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
workflows:
  build:
    jobs:
      - build
