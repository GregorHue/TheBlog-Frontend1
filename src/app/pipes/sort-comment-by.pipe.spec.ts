import { SortCommentByPipe } from './sort-comment-by.pipe';

describe('SortCommentByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCommentByPipe();
    expect(pipe).toBeTruthy();
  });
});
