import { SortPostByPipe } from './sort-post-by.pipe';

describe('SortPostByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPostByPipe();
    expect(pipe).toBeTruthy();
  });
});
